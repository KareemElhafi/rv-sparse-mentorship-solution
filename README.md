RISC-V rv-sparse Mentorship Challenge

This repository contains my solution for the sparse matrix multiplication challenge.

Implementation:
- CSR conversion (row-major scan)
- Sparse matrix-vector multiplication
- No dynamic memory allocation inside the function

How to run:
gcc -lm -o run challenge.c
./run
